// components/Icon.js
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faTrash } from "@fortawesome/free-solid-svg-icons"; // Import your desired icon

const Icon = () => {
  return <FontAwesomeIcon icon={faTrash} />;
};

export default Icon;
